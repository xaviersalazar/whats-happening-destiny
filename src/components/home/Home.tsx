import { Button, Text } from "@nextui-org/react";
import { Box } from "../shared";

const Home = () => (
  <Box
    css={{
      height: "100%",
      padding: "$10",
    }}
  >
    <Text>Home</Text>
    <Button>Click</Button>
  </Box>
);

export default Home;
