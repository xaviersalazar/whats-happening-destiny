const Season = () => {
  // GET curr season ----
  // const { isLoading, data } = useQuery("Settings", fetchDestinySettings);

  // const { data: seasonData } = useQuery("SeasonDefinition", () =>
  //   fetchDestinySeasonDefinition(
  //     data?.Response?.destiny2CoreSettings?.currentSeasonHash
  //   )
  // );

  // if (isLoading) return <Loader />;

  return <h1>Season</h1>;
};

export default Season;
